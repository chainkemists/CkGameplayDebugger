#include "CkDebuggerStyleSettings.h"

// All defaults live inline on the UPROPERTY declarations in the header.
// No constructor here — the base UCk_Plugin_UserSettings_UE only exposes
// an FObjectInitializer-taking constructor and we rely on UE's generated
// one to forward correctly.

// ====================================================================================================================
